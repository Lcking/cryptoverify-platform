HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# Frontend origin for CORS
FRONTEND_ORIGIN=https://app.gambleverify.com

# Public URL when behind reverse proxy (HTTPS)
STRAPI_ADMIN_BACKEND_URL=https://api.gambleverify.com

# Session cookie tuning (temporary mitigation if proxy HTTPS not detected)
# Set SESSION_SECURE=false only to unblock admin login, then revert to true.
SESSION_SECURE=true
SESSION_SAMESITE=lax
# SESSION_DOMAIN=yourdomain.com

# Admin panel session cookie overrides (applies to admin.session and admin.auth.sessions.options.cookie)
ADMIN_SESSION_COOKIE_SECURE=true
ADMIN_SESSION_COOKIE_SAMESITE=lax
# ADMIN_URL=/admin
